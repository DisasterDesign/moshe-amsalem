# DECISIONS — moshe-amsalem

## 2026-07-09 — רדיזיין מלא: מעבר מ-שחור+זהב ל-טורקיז+קרם (מקור: McIntyre Legal)

**הקשר:**
הלקוח (עו"ד משה אמסלם) ביקש קו עיצובי חדש, אנושי וחם יותר, במקום העיצוב
הכהה (`#0A0A0A`) + זהב (`#C9A962`) הקיים. לאחר מחקר ודחיית 3 כיוונים שהוצעו,
הלקוח בחר לקחת את **הצבעים מאתר McIntyre Legal** (`mcintyrelegal.com.au`).

**אופציות שנשקלו:**
- 3 כיווני פלטה שהוצעו (זהב-על-שנהב / אבן-וזית / טרקוטה) — נדחו.
- פלטת McIntyre שחולצה מהאתר בפועל (מדידת צבעים נצפים, לא ברירות-מחדל של Astra).

**ההחלטה:**
פלטת טורקיז light-first שחולצה מ-McIntyre:
- `#153243` טורקיז-נייבי עמוק (סקשנים כהים, sidebar, footer, טקסט ראשי) — מחליף את השחור.
- `#27717F` / `#327B87` טורקיז accent (כפתורים, לינקים, אייקונים) — מחליף את הזהב כ-accent ראשי.
- `#FBFAF7` / `#F3F1EA` קרם חם (רקעי light-first).
- `#153243` ink לטקסט על בהיר.
- הזהב `#C9985E` (מהלוגו) **נשמר** כ-accent משני עדין (רצף מותגי — הלוגו נועל את הזהב).
- ירוק וואטסאפ `#25D366` לכפתור הצף ולבועות הצ'אט.
- פונטים: Rubik → **Assistant (גוף) + Heebo (כותרות)**, self-hosted דרך next/font.

**משמעויות (קוד):**
- `tailwind.config.ts` + `globals.css` — tokens חדשים; שמות legacy (`primary`/`dark`/`light`) מופו-מחדש כדי לצבוע קלאסים קיימים אוטומטית, לצד tokens חדשים (`ink`/`cream`/`gold`/`whatsapp`).
- Hero: הוסר מודל ה-Three.js (מאזני צדק) לטובת פורטרט אמיתי (`/moshe-amsalem.jpeg`, זמני עד קבלת תמונה ייעודית). ה-Hero דה-אנימציה (ללא framer-motion) כדי שתוכן above-the-fold יופיע ללא JS.
- קומפוננטות חדשות: `FloatingWhatsApp`, `Process`, `TestimonialsWhatsApp`, `GoogleReviews`, `HomeContact`.
- עמוד הבית שוכתב: Hero → שירותים → תהליך → המלצות וואטסאפ → ביקורות גוגל → CTA → טופס בתחתית.
- עמודי about/services/contact הוסבו לסכמה הבהירה עם באנר-כותרת טורקיז.

**מועמד לחילוץ?** כן — הפלטה light-first + מבנה תדמית עו"ד/יועץ. אם חוזר ב-2 לקוחות נוספים → primitive.

---

## 2026-07-09 — קרוסלת המלצות בסגנון WhatsApp (hover reveal)

**הקשר:** דרישת הלקוח — הוכחה חברתית מתכתובות וואטסאפ אמיתיות של לקוחות, מעוצבת
(לא צילומי מסך גולמיים) אך אותנטית.

**ההחלטה:** `TestimonialsWhatsApp` — בועות צ'אט מרונדרות ב-HTML אמיתי (RTL, header
ירוק `#075E54`, wallpaper `#ECE5DD`, בועה `#DCF8C6`, טיקים כחולים). ברירת-מחדל =
העיצוב; hover (דסקטופ) / tap (מובייל, נגיש, `aria-pressed`) חושף את **צילום המסך המקורי**.

**סטטוס / TODO:**
- התוכן כרגע **placeholder** (3 המלצות דמו). חובה להחליף בצילומים אמיתיים שחולצו
  מהשיחה עם הלקוח, **עם הסכמה מפורשת בכתב + טשטוש PII** (שמות/טלפונים) לפני עלייה לאוויר.
- לשים את הצילומים ב-`/public/testimonials/` ולהגדיר `realSrc` בכל entry.

**מועמד לחילוץ?** כן — pattern ייחודי ורב-ערך.

---

## 2026-07-09 — ביקורות גוגל: ידני עכשיו, Featurable-headless בהמשך

**הקשר:** דרישת הלקוח — הצגת ביקורות גוגל, בעדיפות לשאיבה אוטומטית.

**אופציות שנשקלו:**
- Google Places API רשמי — **נדחה כברירת-מחדל**: חסום ל-~5 ביקורות, בלי sort, SKU יקר,
  וה-ToS אוסר caching של טקסט הביקורות.
- Featurable (תוכנית חינמית, headless) — שאיבת JSON ורינדור בקומפוננטות שלנו.
- ידני-אוצר (JSON committed, רענון רבעוני).

**ההחלטה:** כרגע **ידני** (`GoogleReviews.tsx`, placeholder) עד קבלת ה-Google Business
Profile / Place ID של הלקוח. יעד: Featurable-headless עם attribution תקין (שם מחבר +
קישור לפרופיל). האתר static-export על Cloudflare Pages — כל fetch יעבור דרך Pages Function
(כמו טופס הצור-קשר הקיים).

**סטטוס / TODO:** לקבל Place ID / קישור לפרופיל; לחווט Featurable או לעדכן ידנית.

**מועמד לחילוץ?** כן (שירות חיצוני — Featurable).

---

## 2026-07-23 — ביקורות גוגל חיות דרך Places API (New) + Pages Function (מחליף את ההחלטה הקודמת)

**הקשר:**
הלקוח ביקש חיבור API מתעדכן יומי לדירוג ולביקורות, בקרוסלה בדף הבית, כולל
כפתור לכתיבת ביקורת. ההחלטה הקודמת (ידני / Featurable) מוחלפת.

**אופציות שנשקלו:**
- Featurable headless — עדיין דורש הרשמה לצד-שלישי, ותלות בשירות חינמי.
- Places API (New) דרך Pages Function — first-party, בלי תלות חיצונית.
- שמירה על ידני — נדחה, הלקוח ביקש עדכון אוטומטי.

**ההחלטה:**
`functions/api/reviews.ts` — Cloudflare Pages Function שקוראת ל-Places API (New),
מחזירה JSON מנורמל, ומקאשת **24 שעות** ב-edge cache (`caches.default`).
המפתח נשאר בצד השרת ולא נחשף בדפדפן. `?refresh=1` עוקף קאש.

- Place resolution: `GOOGLE_PLACE_ID` אם קיים, אחרת Text Search לפי `GOOGLE_PLACE_QUERY`.
- סינון: מוצגות רק ביקורות עם דירוג ≥ 4 וטקסט לא ריק ("התגובות הנבחרות").
- `GoogleReviews.tsx` הפך לקרוסלה, מושך מה-API ב-`useEffect`, **ונופל חזרה
  לסנאפשוט מוטמע** (5 ביקורות, יולי 2026) אם ה-API לא זמין — הסקשן לעולם לא ריק.
- כפתור "כתבו ביקורת בגוגל": ה-API מחזיר `writeReviewUrl` מדויק לפי place id;
  ה-fallback משתמש ב-`.../data=!4m3!3m2!1s<FEATURE_ID>!12e1` שפותח את דיאלוג
  כתיבת הביקורת ישירות.

**משמעויות:**
- דורש `GOOGLE_PLACES_API_KEY` ב-Cloudflare Pages (ראה `.env.example`).
- מגבלת Google: **עד 5 ביקורות** מה-API, בלי שליטה על מיון. מעבר לכך נדרש
  צד-שלישי. כרגע ללקוח יש בדיוק 5, אז אין פער.
- SKU: Place Details Enterprise+Atmosphere. עם קאש יומי — ~30-60 קריאות/חודש,
  הרבה מתחת ל-free tier.
- attribution נשמר: שם המחבר, תמונה, קישור לפרופיל וקישור לביקורת.

**מועמד לחילוץ?** כן — primitive `google-reviews` (Pages Function + קרוסלה + fallback).

---

## 2026-07-23 — מפתח Google API נפרד לכל אתר לקוח (לא משותף)

**הקשר:**
ה-Function דורשת `GOOGLE_PLACES_API_KEY`. באתר האחות fuzionwebz רץ אותו pattern
עם מפתח קיים בפרויקט Google Cloud של פיוז'ן. עלתה השאלה אם להשתמש באותו מפתח
לשני האתרים.

**ההחלטה:**
מפתח **נפרד לכל אתר לקוח**, באותו פרויקט Google Cloud ("My First Project",
`project-580771e3-c431-463a-999`). נוצר `ams-law-places`, מוגבל ב-API restrictions
ל-Places API (New) בלבד, עם Application restrictions = **None** (קריאת שרת בלי
referrer — הגבלת referrer הייתה שוברת אותו). נשמר כ-**Secret** ב-Cloudflare Pages
של moshe-amsalem.

**נימוק:**
- רדיוס נזק: מפתח משותף = נקודת כשל אחת; דליפה/סיבוב מפיל את כל האתרים יחד.
- נראות: שימוש בגוגל נמדד per-key; מפתח נפרד = לוגים ומדדים נקיים per-אתר.
- בעלות: התשתית של פיוז'ן לא נשזרת אצל אתר לקוח.
- **הבהרה:** מפתח נפרד נותן סיבוב/ביטול/נראות עצמאיים, אבל **לא** מפריד חיוב או
  מכסות — אלה ברמת הפרויקט. הפרדת חיוב מלאה דורשת פרויקט Google Cloud נפרד לכל לקוח.

**כלל אצבע לסוכנות:** פרויקט אחד, מפתח נפרד לכל אתר; פרויקט נפרד רק כשהלקוח צריך
בעלות מלאה על התשתית/החיוב.

**מועמד לחילוץ?** כן — חלק מ-primitive `google-reviews` + פרוטוקול bootstrap לקוח.

---

## 2026-08-18 — שדרוג לפי מסמך התיקונים של הלקוח: הדר עליון, עמודי תוכן, רגולציה

**הקשר:**
הלקוח שלח מסמך תיקונים מפורט (Google Doc, 5 פרקים) שדרש להעלות את האתר מרמת
"נקי אבל דליל" לרמת אתרי הרפרנס limorarbel.com, dsegev-law.co.il ו-
zilberman-law.co.il: יותר סמכות, יותר תוכן, יותר אלמנטי אמון. כפוף לשמירה על
הפלטה הקיימת (נייבי-טורקיז + זהב + קרם), איסור אימוג'ים, ואיסור מוחלט על
המצאת נתונים.

**אופציות שנשקלו:**
- תיקון נקודתי של הסקשנים הקיימים — נדחה, המבנה (sidebar קבוע, עמוד שירותים
  יחיד) לא יכול היה לגדול ל-6 עמודי תחום + בלוג + פרויקטים.
- שכתוב מלא של האתר מאפס — נדחה, פלטה, טפסים, ביקורות גוגל וההמלצות עובדים.
- **נבחר:** החלפת שכבת המבנה (ניווט, אנימציות, מטא) + הוספת שכבת תוכן
  (`content/*`) על גבי הקומפוננטות הקיימות.

**ההחלטה ומשמעויותיה:**

1. **Sidebar → Header עליון דביק.** ה-sidebar אכל 288px מכל viewport ולא היה
   בו מקום ל-6 עמודי תחום. הוחלף ב-`Header` sticky שמתכווץ בגלילה
   (h-24 → h-16), עם dropdown לתחומי עיסוק ו-drawer במובייל.
   `components/Sidebar.tsx` נמחק. `layout.tsx` איבד את ה-`md:mr-72`.

2. **framer-motion → `components/Reveal.tsx`.** תלונת הלקוח על סקשנים
   "ריקים/דהויים כמה שניות" נבעה מ-`whileInView` עם `delay: index * 0.1`
   מצטבר. הוחלף ב-IntersectionObserver + CSS transition: 350ms, 12px,
   stagger מוגבל ל-3 צעדים. הקלאס `js` מוזרק ב-`<head>` כדי שהתוכן יישאר
   גלוי אם ה-bundle נכשל. מכבד `prefers-reduced-motion` ואת ווידג'ט הנגישות.
   `framer-motion` כבר לא מיובא בשום מקום (הוסרה גם `ScalesModel.tsx`
   שהייתה dead code) — הסרת התלות מ-`package.json` נשארה כ-follow-up נפרד.

3. **שכבת תוכן נפרדת (`content/`).** כל הקופי הארוך (6 תחומים, 6 מאמרים,
   3 עמודים משפטיים, FAQ, ציר זמן) יושב כ-data מוטיפס ב-`content/*.ts`,
   והעמודים הם תבניות. זה מה שאיפשר לייצר את התוכן במקביל ולהחליף אותו
   בלי לגעת ב-JSX. `config/site.ts` הוא מקור אמת יחיד לפרטי קשר.

4. **Placeholders במקום נתונים מומצאים.** כל נתון שהלקוח עוד לא מסר מרונדר
   כ-chip מקווקו `[... - לקבל ממשה]` ומרוכז ב-`PENDING` ב-`config/site.ts`
   וב-`content/projects.ts`. הקאונטרים מנפישים רק ערך אמיתי; היחיד כזה היום
   הוא דירוג הגוגל, שנמשך מ-`/api/reviews`.

5. **`lib/reviews.ts` — hook משותף.** באדג' הביקורות הצף וסקשן הביקורות
   קוראים לאותו hook עם module-level cache, כך שהעמוד מבצע fetch אחד ולא שניים.

6. **Turnstile: כפתור submit לא ננעל יותר.** באג bfe0c99 חזר בפוטנציה עם
   3 טפסים חדשים. `disabled` מוגבל עכשיו ל-`isLoading` בלבד; חוסר הסכמה או
   חוסר טוקן מוחזרים כהודעת שגיאה מפורשת. אין מסלול שבו הכפתור מת לצמיתות.

7. **`/api/contact`: email אופציונלי + consent חובה + honeypot.** הטופס המהיר
   בהירו אוסף שם וטלפון בלבד, ולכן שדה המייל נבדק רק אם נמסר. `consent !== true`
   מוחזר כ-400. שדה `company` נסתר הוא honeypot שמחזיר 200 שקט לבוטים.

8. **`/api/lead-magnet` — endpoint חדש.** מייל התראה למשרד + מייל אספקה לפונה.
   כשה-env `LEAD_MAGNET_URL` ריק, הפונה מקבל הודעה שהמסמך יישלח, במקום קישור מת.

**סטיות מהספרייה הקנונית:**
- `a11y-widget` הועתק מ-`~/Projects/Foundation/code-library/a11y-widget` ועבר
  fork: מיפוי `brand-*` → tokens של הפרויקט, טריגר ה-emoji ♿ הוחלף ב-SVG
  (הלקוח אסר אימוג'ים), ונוספו גופן קריא / הדגשת קישורים / סמן גדול לכיסוי
  מלא יותר של ת״י 5568 AA.
- `seo-primitives/sitemap.ts` לא הועתק — הוא נשען על Prisma, והאתר סטטי בלי DB.
  נכתב sitemap סטטי במקומו. `robots.ts` הועתק כמעט כפי שהוא.

**חובות פתוחים (חוסמים עלייה לאוויר):**
- מספרי הקאונטרים, פרטי 3 הפרויקטים, הסיפור האישי לעמוד אודות, סריקות תעודות,
  קובץ ה-PDF של הצ׳קליסט, רכז נגישות, פרטי נגישות פיזית, דוא״ל לפניות פרטיות,
  ותקופת שמירת פניות שלא הבשילו.
- 6 המאמרים נכתבו כטיוטות בית (בלי שום מספר, שיעור או מועד סטטוטורי) עד
  שיתקבל התוכן שהלקוח אמור לספק.

**מועמד לחילוץ?**
כן, שלושה:
- `Reveal` + הקלאס `js` — פתרון scroll-reveal בלי ספריית אנימציה ובלי FOUC.
- `PENDING` + `.pending-value` — קונבנציה להצגת נתון חסר בלי להמציא אותו.
  רלוונטי לכל לקוח Floor שנמסר לפני שהתוכן שלם.
- חבילת הרגולציה הישראלית (הצהרת נגישות + ווידג'ט + מדיניות פרטיות + באנר
  עוגיות + תקנון) — חוזרת בכל לקוח ישראלי. מועמד ברור ל-primitive
  `il-compliance-pack` אחרי לקוח שני.
